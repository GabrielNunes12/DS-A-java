package org.example.TreeDataStructureIntro;

public class Trees {
  public static void main(String[] args) {
    //Tree -> a non-linear data structure where nodes and edges are organized in a hierarchy
    // a real example of this, could be a family tree
    // abstract examples: File explorer, Databases, DNS and HTML DOM
    // top of a tree is the root node
    //                            root
    //                            (A)
    //            branch -> (B)          (C)    <- Branch
    //         SIBLINGS: (D)  -> (E)    ->    (F)
    //                (leaf)    (leaf)         (leaf)
    // Size of a tree = # of nodes
    // depth of a tree = # of edges below the root node
    // height = # of edges above furthest leaf node
  }
}
